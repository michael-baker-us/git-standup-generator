"""Shared pytest fixtures.

Populated in milestone M1+ (sample commits, fake git runner, temp-repo helper).
Kept minimal for now so the test directory exists and tooling has a target.
"""

from __future__ import annotations
